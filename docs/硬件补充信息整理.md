# 硬件补充信息整理

更新时间：2026-04-26 20:29:20

本文整理自 `docs/需要补充的信息.md` 中已经由用户补充完成的内容。后续固件和硬件匹配任务应优先参考本文，再结合 `docs/硬件匹配分析.md`、`docs/pin_map.md`、`项目背景/` 下原理图和数据手册执行。

## 1. 已确认信息

| 编号 | 项目 | 用户补充结果 | 对后续任务的影响 |
|---|---|---|---|
| H01 | MCU / 模组型号 | `ESP32-S3-WROOM-1-N16R8` | 后续判断 GPIO、Flash/PSRAM、板载资源时以该模组为准。 |
| H02 | DevKit 插接方向 | DevKit 与原理图 `Left/Right` 完全一致；以 Wi-Fi 天线在上的方向分左右。 | `docs/硬件匹配分析.md` 中的排针到 GPIO 推断可以作为后续映射基础。 |
| H03 | 网络到 GPIO 连通性 | 已用万用表复核。 | Task 01 可把推断 GPIO 落到 `docs/pin_map.md` 和 `src/bsp/bsp_pins.h`，但仍建议在文档中标注“已由用户复核”。 |
| H04 | GPIO38 / RGB LED 风险 | 板载 LED 真实使用 GPIO47，且实际不使用该 LED。 | `GPIO38` 可继续作为 `STB3` 候选，不再把 RGB LED 视为阻塞风险。 |
| H05 | GPIO35~40 可用性 | GPIO35~40 可以作为普通 GPIO 稳定输出。 | `STB1~STB6` 可以继续按 GPIO40/39/38/37/36/35 推进。 |
| H06 | `G_VH` 控制方向 | 高通低断。 | 固件中 `G_VH` 高电平打开 `VH_OUT`、低电平关闭 `VH_OUT` 的方向成立。 |
| H09 | `EQD` 分压网络 | 按 220k/100k 分压处理。 | 电池电压换算可先按约 3.2 倍计算，并保留 ADC 校准 TODO。 |
| H11 | FPC 插入方向 | 用户确认实际没有插反；仍可由 Codex 根据原理图和数据手册再核对。 | 后续可以继续按当前 FPC 信号定义推进。 |
| H13 | 电池方案 | 2S 方案，两个 18650 串联。 | 低电压保护、电池电压显示和打印限制按 2S 锂电池设计。 |
| H14 | H3 供电影响 | H3 不影响 MCU 供电，只是在烧录时断开板子供电，防止对流/反灌。 | 固件无需为 H3 做运行期控制；文档可说明它是硬件烧录/供电隔离用途。 |
| H15 | 调试通信方式 | 不使用串口；如需调试，使用 WiFi 传输并设置测试 API。设备局域网 IP：`192.168.1.134`。 | 后续 `SENSOR_TEST/STATUS/SAFE_OFF` 验证应增加 WiFi 测试 API 方案，不能只依赖串口工具。 |

## 2. 已决定先忽略或延后实测的事项

| 编号 | 项目 | 当前处理方式 | 后续 TODO |
|---|---|---|---|
| H07 | `PAPER_N` 有纸/无纸电平 | 先忽略，固件阶段可先按“始终有纸”处理。 | 后续拿到实测电平后，再接入真实缺纸检测。 |
| H08 | `BAT_STAT` 状态语义 | 查看 IP2326 数据手册确定。 | 后续 Task 09 中按数据手册落实，不要在未确认前猜测。 |
| H10 | `TM1` ADC 原始值 | 先忽略，等待用户提供 ADC 原始值。 | 真实 NTC 温度换算先保留 TODO，避免温度保护误判。 |
| H12 | 电机正向走纸相序 | 先查看数据手册；若没有明确相序，则先跑起来再实测方向。 | MOTOR_TEST 必须低速、小步数，并允许根据实测调整方向。 |

## 3. 对待办任务的直接修正点

- Task 01：GPIO 映射可以按已复核结果落地，不再要求“不能宣称已硬件验证”；应改为“标注已由用户完成连通性复核”。
- Task 02：真实传感器驱动第一阶段不要强行接入 `PAPER_N` 和 `TM1` 的真实判断；`PAPER_N` 可先固定为有纸并写 TODO，`TM1` 可先保留 mock/保守值并等待 ADC 原始值。
- Task 03：验证通道应从“真实串口验证流程”改为“WiFi 测试 API 验证流程”；设备 IP 可先按 `192.168.1.134` 写入测试说明。
- Task 04：`G_VH` 方向已确认高通低断，但仍必须保留 SAFE_OFF 和空载波形验证。
- Task 09：`EQD` 按 220k/100k 分压推进；`BAT_STAT` 必须查 IP2326 数据手册后再落地。

## 4. 仍需保持谨慎的点

- `PAPER_N` 未实测前，不要把缺纸保护宣称为已完成。
- `TM1` 未提供 ADC 原始值前，不要把真实过温保护宣称为已完成。
- WiFi 测试 API 还没有在固件中实现，不能直接替代现有 UART 工具，需要作为后续任务新增。
- 首次真实加热打印仍必须按低密度、短行数、可立即断电的方式进行。
