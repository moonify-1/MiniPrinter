# 修改记录

## 2026-04-24 12:25:08

- 执行 Step 00：仓库体检与任务文档落盘。
- 检查了当前仓库树，确认已存在 PlatformIO 工程骨架。
- 新建 `docs/firmware_task_plan.md`、`docs/architecture_notes.md`、`docs/pin_map.md`、`docs/codex_progress.md`。
- 建立 `docx/项目结构.md` 与本运行记录文件。
- 未修改 `platformio.ini`。
- 未编写真实硬件驱动。
- 未创建打印头加热逻辑。

## 2026-04-24 12:33:24

- 执行 Step 02：公共类型、错误码和状态枚举。
- 新增 `src/common/app_types.h`。
- 新增 `src/common/app_error.h`、`src/common/app_error.cpp`。
- 修改 `src/main.cpp`，仅保留最小 include 与链接验证。
- 重排 `src/common/` 与 `src/main.cpp` 的注释和枚举格式，提升可读性。
- 更新 `docs/codex_progress.md`。
- 修正运行记录目录，确保记录文件位于 `docx/`。
- 使用 `python -m platformio run` 编译通过。
- 确认 `src/common/` 未包含 `Arduino.h`、`FreeRTOS.h`。
- 未引入 Arduino/FreeRTOS 到 `common` 层。
- 未编写真实硬件驱动。
- 未创建打印头加热逻辑。

## 2026-04-24 12:50:00

- 执行 Step 03：配置系统骨架和安全阈值。
- 新增 `src/config/project_features.h`。
- 新增 `src/config/print_config.h`。
- 新增 `src/config/safety_limits.h`。
- 新增 `src/config/default_params.h`。
- 修改 `src/main.cpp`，加入配置头文件的最小编译验证。
- 更新 `docs/codex_progress.md`。
- 更新 `docx/项目结构.md`。
- `config` 层未引入 `Arduino.h`、`FreeRTOS.h`。
- 使用 `python -m platformio run` 编译通过。
- 未编写真实硬件驱动。
- 未创建打印头加热逻辑。

## 2026-04-24 12:59:05

- 执行 Step 04：BSP Pin Map 与安全输出入口。
- 新增 `src/bsp/bsp_pins.h`。
- 新增 `src/bsp/bsp_board.h`、`src/bsp/bsp_board.cpp`。
- 新增 `src/bsp/bsp_gpio.h`、`src/bsp/bsp_gpio.cpp`。
- 修改 `src/main.cpp`，最早调用 `Bsp_PreInitSafeOutputs()`，然后再执行 `Serial.begin()`。
- 更新 `docs/codex_progress.md`。
- 更新 `docx/项目结构.md`。
- 当前不会打开 VH，不会输出 STB 有效电平，不会唤醒 DRV8833。
- 未分配引脚统一跳过，不报错。
- 使用 `python -m platformio run` 编译通过。

## 2026-04-24 13:06:00

- 执行 Step 05：RTOS 对象与 Task Registry。
- 新增 `src/rtos/rtos_objects.h`、`src/rtos/rtos_objects.cpp`。
- 新增 `src/rtos/task_registry.h`、`src/rtos/task_registry.cpp`。
- 修改 `src/main.cpp`，在 `Bsp_Init()` 后调用 `Rtos_CreateObjects()`，失败则切回安全输出并打印错误。
- 更新 `docs/codex_progress.md`。
- 更新 `docx/项目结构.md`。
- 当前未创建实际任务。
- 当前队列元素使用 `uint32_t` 占位，后续需替换为真实消息结构。
- 使用 `python -m platformio run` 编译通过。

## 2026-04-24 13:14:14

- 执行 Step 06：异步日志系统 MVP。
- 新增 `src/services/log_service.h`、`src/services/log_service.cpp`。
- 新增 `src/tasks/task_log.h`、`src/tasks/task_log.cpp`。
- 修改 `src/rtos/rtos_objects.cpp`，将 `qLog` 改为真实 `LogMsg` 队列。
- 修改 `src/main.cpp`，在 RTOS 对象创建后初始化日志，并创建 `LogTask`。
- 更新 `docs/codex_progress.md`。
- 更新 `docx/项目结构.md`。
- 当前日志消息为固定长度结构，未使用动态分配。
- 使用 `python -m platformio run` 编译通过。

## 2026-04-24 13:20:00

- 执行 Step 07：MonitorTask 与心跳框架。
- 新增 `src/services/health_service.h`、`src/services/health_service.cpp`。
- 新增 `src/tasks/task_monitor.h`、`src/tasks/task_monitor.cpp`。
- 修改 `src/tasks/task_log.cpp`，改为通过 `Health_ReportHeartbeat()` 上报心跳。
- 修改 `src/rtos/task_registry.h`、`src/rtos/task_registry.cpp`，加入 `TaskId::MONITOR` 并补充注册时间戳。
- 修改 `src/main.cpp`，在日志初始化后初始化健康服务，并创建 `MonitorTask`。
- 更新 `docs/codex_progress.md`。
- 更新 `docx/项目结构.md`。
- 当前未启用真实硬件 Watchdog。
- 使用 `python -m platformio run` 编译通过。

## 2026-04-24 13:31:00

- 执行 Step 08：SystemTask 与系统状态机骨架。
- 新增 `src/app/app_system.h`、`src/app/app_system.cpp`。
- 新增 `src/tasks/task_system.h`、`src/tasks/task_system.cpp`。
- 修改 `src/services/health_service.cpp`，将 SystemTask 纳入关键心跳检查。
- 修改 `src/main.cpp`，在 MonitorTask 之前创建 SystemTask。
- 更新 `docs/codex_progress.md`。
- 更新 `docx/项目结构.md`。
- 当前状态机初始路径为 `BOOT -> INIT -> SELF_TEST -> IDLE`。
- SAFE_MODE 下仍然只做安全输出收敛，不开启任何危险输出。
- 使用 `python -m platformio run` 编译通过。

## 2026-04-24 15:57:05

- 执行 Step 09：Driver 接口与 Mock Driver。
- 新增 `src/drivers/thermal_head/drv_thermal_head.h`、`src/drivers/thermal_head/drv_thermal_head_mock.cpp`。
- 新增 `src/drivers/stepper/drv_stepper.h`、`src/drivers/stepper/drv_stepper_mock.cpp`。
- 新增 `src/drivers/sensors/drv_sensor_types.h`、`src/drivers/sensors/drv_sensors_mock.cpp`。
- 当前 mock driver 不操作 GPIO，不包含真实 ESP32 驱动。
- 当前 driver 接口只描述硬件动作，不包含业务决策。
- 更新 `docs/codex_progress.md`。
- 更新 `docx/项目结构.md`。
- 使用 `python -m platformio run` 编译通过。

## 2026-04-24 16:03:41

- 执行 Step 10：SensorService 与 SensorTask。
- 新增 `src/services/sensor_service.h`、`src/services/sensor_service.cpp`。
- 新增 `src/tasks/task_sensor.h`、`src/tasks/task_sensor.cpp`。
- 修改 `src/rtos/rtos_objects.cpp`，将 `qSensor` 改为长度为 1 的 `SensorSnapshot` 队列。
- 修改 `src/services/health_service.cpp`，将 SensorTask 纳入关键心跳检查。
- 修改 `src/main.cpp`，创建 SensorTask 后再创建 MonitorTask。
- SensorService 会更新纸张、温度、电池和驱动就绪事件位。
- 使用 `python -m platformio run` 编译通过。

## 2026-04-24 16:10:08

- 执行 Step 11：协议基础：CRC、帧解析、PING/STATUS。
- 新增 `src/protocol/proto_crc.h`、`src/protocol/proto_crc.cpp`。
- 新增 `src/protocol/proto_frame.h`、`src/protocol/proto_cmd.h`。
- 新增 `src/protocol/proto_parser.h`、`src/protocol/proto_parser.cpp`。
- 新增 `src/services/protocol_service.h`、`src/services/protocol_service.cpp`。
- 新增 `src/tasks/task_protocol.h`、`src/tasks/task_protocol.cpp`。
- 新增 `src/tasks/task_command.h`、`src/tasks/task_command.cpp`。
- 修改 `src/rtos/rtos_objects.cpp`，将 `qCommand` 改为 `ProtoFrame` 队列。
- 修改 `src/main.cpp`，创建 CommandTask 和 ProtocolTask。
- 当前只支持 PING、GET_INFO、GET_STATUS，不实现打印命令。
- 使用 `python -m platformio run` 编译通过。

## 2026-04-24 16:16:08

- 执行 Step 12：参数系统 ParamService。
- 新增 `src/drivers/storage/drv_nvs.h`、`src/drivers/storage/drv_nvs.cpp`。
- 新增 `src/services/param_service.h`、`src/services/param_service.cpp`。
- 新增 `src/tasks/task_param.h`、`src/tasks/task_param.cpp`。
- 修改 `src/main.cpp`，启动时初始化并加载参数，随后创建 ParamTask。
- 修改 `src/protocol/proto_cmd.h`、`src/services/protocol_service.cpp`，增加参数相关命令入口。
- 参数保存通过 `qParam` 延迟合并，打印活动期间不立即写 Flash。
- NVS 不可用或参数校验失败时回退默认参数。
- 使用 `python -m platformio run` 编译通过。
- 按要求创建 git commit。

## 2026-04-24 16:21:05

- 执行 Step 13：固定内存池、LineBuffer 与打印数据 Spooler。
- 新增 `src/common/fixed_pool.h`、`src/common/fixed_pool.cpp`。
- 新增 `src/services/print_spooler.h`、`src/services/print_spooler.cpp`。
- 修改 `src/rtos/rtos_objects.cpp`，将 `qLineFree` 和 `qLineReady` 改为 `LineBuffer*` 队列。
- 修改 `src/main.cpp`，启动时初始化 PrintSpooler。
- 修改 `src/protocol/proto_cmd.h`、`src/services/protocol_service.cpp`，接入 `PRINT_LINE` 命令。
- `PRINT_LINE` 只入队 48 字节行数据，不启动真实打印。
- 队列满或无空闲行时返回 `ERR_QUEUE_FULL`。
- 使用 `python -m platformio run` 编译通过。
- 按要求创建 git commit。

## 2026-04-24 16:25:18

- 执行 Step 14：ThermalSafetyService。
- 新增 `src/services/thermal_safety_service.h`、`src/services/thermal_safety_service.cpp`。
- 实现打印安全前置检查、热脉冲计算和行缓冲分组检查。
- 安全规则覆盖缺纸、温度无效、过温、低电压、单组黑点数和热脉冲硬上限。
- 当前只返回 `AppErrorCode` 或计算结果，不进入 SAFE_MODE。
- 当前不包含 `Arduino.h`，不操作 GPIO。
- 使用 `python -m platformio run` 编译通过。
- 按要求创建 git commit。

## 2026-04-24 16:30:20

- 执行 Step 15：真实 ThermalHead Driver 的安全骨架。
- 新增 `src/bsp/bsp_spi.h`、`src/bsp/bsp_spi.cpp`。
- 新增 `src/bsp/bsp_timer.h`、`src/bsp/bsp_timer.cpp`。
- 新增 `src/drivers/thermal_head/drv_thermal_head_esp32.cpp`。
- 修改 `src/drivers/thermal_head/drv_thermal_head.h`，增加 mock driver 获取入口和 debug waveform test 声明。
- 修改 `src/drivers/thermal_head/drv_thermal_head_mock.cpp`，默认硬件关闭时继续返回 mock driver。
- 修改 `src/common/app_error.h`、`src/common/app_error.cpp`，增加 `ERR_HW_DISABLED`。
- 默认 `MP_ENABLE_HW_THERMAL_HEAD=0` 时不操作真实 GPIO/SPI。
- 真实硬件路径只在 `MP_ENABLE_HW_THERMAL_HEAD=1` 时编译启用。
- 使用 `python -m platformio run` 编译通过。
- 额外使用 `PLATFORMIO_BUILD_FLAGS=-DMP_ENABLE_HW_THERMAL_HEAD=1` 编译通过，期间修正 ESP32 Arduino 的 SPI 位序常量为 `SPI_MSBFIRST`。
- 按要求创建 git commit。

## 2026-04-24 16:38:44

- 执行 Step 16：DRV8833 Stepper Driver 安全实现。
- 新增 `src/drivers/stepper/drv_stepper_drv8833.cpp`。
- 修改 `src/drivers/stepper/drv_stepper.h`，增加 `GetStepperMockDriver()`。
- 修改 `src/drivers/stepper/drv_stepper_mock.cpp`，默认硬件关闭时继续返回 mock driver。
- 真实 DRV8833 GPIO 操作只在 `MP_ENABLE_HW_STEPPER=1` 时编译启用。
- 实现 nSLEEP 安全休眠、1ms 唤醒等待、nFAULT 低有效故障检测和 4-step 全步相序。
- 本步骤没有创建 task，没有接入 PrintEngineTask，没有自动执行电机测试。
- 使用 `python -m platformio run` 编译通过。
- 额外使用 `PLATFORMIO_BUILD_FLAGS=-DMP_ENABLE_HW_STEPPER=1` 编译通过。
- 按要求创建 git commit。

## 2026-04-24 16:48:09

- 执行 Step 17：PrintEngineTask 第一版，只跑 Mock 打印流程。
- 新增 `src/app/app_print.h`、`src/app/app_print.cpp`。
- 新增 `src/services/print_service.h`、`src/services/print_service.cpp`。
- 新增 `src/tasks/task_print_engine.h`、`src/tasks/task_print_engine.cpp`。
- 修改 `src/rtos/rtos_objects.cpp`，将 `qPrintCtrl` 改为 `PrintControlMsg` 队列。
- 修改 `src/protocol/proto_cmd.h`、`src/services/protocol_service.cpp`，增加 `PRINT_START`、`PRINT_END`、`PRINT_CANCEL`、`FEED`。
- 修改 `src/services/health_service.cpp`，将 PrintEngineTask 纳入关键任务心跳检查。
- 修改 `src/main.cpp`，初始化 PrintService 并创建 PrintEngineTask。
- PrintEngineTask 只调用 mock ThermalHeadDriver 和 mock StepperDriver，不打开真实 VH/STB/电机。
- 使用 `python -m platformio run` 编译通过。
- 额外使用 `PLATFORMIO_BUILD_FLAGS="-DMP_ENABLE_HW_THERMAL_HEAD=1 -DMP_ENABLE_HW_STEPPER=1"` 编译通过。
- 按要求创建 git commit。

## 2026-04-24 16:56:30

- 执行 Step 18：真实打印硬件接入保护层。
- 修改 `src/tasks/task_print_engine.cpp`，加入真实打印路径保护和逐行真实流程。
- 修改 `src/services/print_service.h`、`src/services/print_service.cpp`，新增真实硬件启用判断和真实打印许可检查。
- 修改 `src/services/thermal_safety_service.cpp`，将电机故障纳入禁止加热条件。
- 修改 `src/drivers/thermal_head/drv_thermal_head.h`、`drv_thermal_head_mock.cpp`、`drv_thermal_head_esp32.cpp`，让关键硬件动作返回是否成功。
- 默认宏关闭时仍可 mock 打印，不操作真实 VH/STB/电机。
- 真实路径要求热敏头和步进电机硬件宏同时开启，且系统状态、传感器、纸张、温度、电池和电机故障状态全部通过。
- 任意步骤失败会关闭 STB/VH、释放电机、执行 BSP 安全输出、投递 `qError`。
- 使用 `python -m platformio run` 编译通过。
- 额外使用 `PLATFORMIO_BUILD_FLAGS="-DMP_ENABLE_HW_THERMAL_HEAD=1 -DMP_ENABLE_HW_STEPPER=1"` 编译通过。
- 按要求创建 git commit。
